import * as React from 'react';
import {View, Text} from 'react-native';
import { FormInput } from '../components/FormBiodata/FormInput'
export default HomeScreen = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <FormInput/>
    </View>
  );
};
